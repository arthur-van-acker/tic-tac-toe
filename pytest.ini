[pytest]
minversion = 7.0
addopts =
    --strict-config
    --strict-markers
    -ra
    --cov=tictactoe
    --cov-report=term
    --cov-fail-under=50
markers =
    gui: marks tests that require a GUI/display (deselect with '-m "not gui"')
testpaths =
    tests
